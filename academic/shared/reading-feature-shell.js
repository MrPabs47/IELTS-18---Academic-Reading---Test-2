(function(global) { "use strict"; var validatedConfig = null; var initialized = false; var lastError = ""; var elements = null; var studyIntervalId = null; var studyElapsedSeconds = 0; var studySessionActive = false; var previouslyFocusedElement = null; var renderedResultVersion = 0; var studySessionResultBaseline = 0; var latestRenderedScoreGuideResult = null; var resultObserver = null; function isPlainObject(value) { return Object.prototype.toString.call(value) === "[object Object]"; } function hasFunction(container, key) { return Boolean(container && typeof container[key] === "function"); } function isNonEmptyString(value) { return typeof value === "string" && Boolean(value.trim()); } function validatePartRange(range, label) { if (!isPlainObject(range)) { return "test.partRanges." + label + " must be an object."; } if (!Number.isInteger(range.from) || !Number.isInteger(range.to)) { return "test.partRanges." + label + " must include integer from/to values."; } if (range.from < 1 || range.to < range.from) { return "test.partRanges." + label + " must be a valid question range."; } return ""; } function validateScoreGuide(scoreGuide) { if (!isPlainObject(scoreGuide)) { return "ReadingFeatureShell config.study.scoreGuide must be an object."; } if (!isNonEmptyString(scoreGuide.title)) { return "ReadingFeatureShell config.study.scoreGuide.title must be a non-empty string."; } if (!isNonEmptyString(scoreGuide.intro)) { return "ReadingFeatureShell config.study.scoreGuide.intro must be a non-empty string."; } if (!Array.isArray(scoreGuide.rows) || scoreGuide.rows.length === 0) { return "ReadingFeatureShell config.study.scoreGuide.rows must be a non-empty array."; } for (var i = 0; i < scoreGuide.rows.length; i += 1) { var row = scoreGuide.rows[i]; if (!isPlainObject(row) || !isNonEmptyString(row.correctAnswers) || !isNonEmptyString(row.band)) { return "ReadingFeatureShell config.study.scoreGuide.rows must include non-empty correctAnswers and band strings."; } } return ""; } function validateConfig(config) { var requiredParts = ["1", "2", "3"]; var error = ""; if (!isPlainObject(config)) { error = "ReadingFeatureShell config must be an object."; } else if (config.version !== 1) { error = "ReadingFeatureShell config.version must be 1."; } else if (!isPlainObject(config.test)) { error = "ReadingFeatureShell config.test must be an object."; } else if (typeof config.test.id !== "string" || !config.test.id.trim()) { error = "ReadingFeatureShell config.test.id must be a non-empty string."; } else if (typeof config.test.title !== "string" || !config.test.title.trim()) { error = "ReadingFeatureShell config.test.title must be a non-empty string."; } else if (config.test.totalQuestions !== 40) { error = "ReadingFeatureShell config.test.totalQuestions must be 40."; } else if (!isPlainObject(config.test.partRanges)) { error = "ReadingFeatureShell config.test.partRanges must be an object."; } else if (!isPlainObject(config.state)) { error = "ReadingFeatureShell config.state must be an object."; } else if (!hasFunction(config.state, "getMode")) { error = "ReadingFeatureShell config.state.getMode must be a function."; } else if (!hasFunction(config.state, "isTestSubmitted")) { error = "ReadingFeatureShell config.state.isTestSubmitted must be a function."; } else if (!hasFunction(config.state, "getActivePart")) { error = "ReadingFeatureShell config.state.getActivePart must be a function."; } else if (!isPlainObject(config.answers) || !hasFunction(config.answers, "getAnswerKeyDisplay")) { error = "ReadingFeatureShell config.answers.getAnswerKeyDisplay must be a function."; } else if (!isPlainObject(config.navigation) || !hasFunction(config.navigation, "getQuestionTarget")) { error = "ReadingFeatureShell config.navigation.getQuestionTarget must be a function."; } else if (!isPlainObject(config.study)) { error = "ReadingFeatureShell config.study must be an object."; } else { error = validateScoreGuide(config.study.scoreGuide); } if (!error) { for (var i = 0; i < requiredParts.length; i += 1) { error = validatePartRange(config.test.partRanges[requiredParts[i]], requiredParts[i]); if (error) break; } } return { ok: !error, error: error }; } function formatStudyTime(totalSeconds) { var minutes = Math.floor(totalSeconds / 60); var seconds = totalSeconds % 60; return String(minutes).padStart(2, "0") + ":" + String(seconds).padStart(2, "0"); } function updateStudyTimerText() { if (elements && elements.studyTimerValue) { elements.studyTimerValue.textContent = formatStudyTime(studyElapsedSeconds); } } function stopStudyTimer() { if (studyIntervalId) { global.clearInterval(studyIntervalId); studyIntervalId = null; } } function startStudyTimer() { stopStudyTimer(); studyIntervalId = global.setInterval(function() { if (!studySessionActive) return; studyElapsedSeconds += 1; updateStudyTimerText(); }, 1000); } function createElement(tagName, className, text) { var element = global.document.createElement(tagName); if (className) element.className = className; if (typeof text === "string") element.textContent = text; return element; } function normaliseScoreGuideResult(value) { if (!isPlainObject(value)) return null; if (!Number.isInteger(value.rawScore) || !validatedConfig || value.rawScore < 0 || value.rawScore > validatedConfig.test.totalQuestions) { return null; } if (typeof value.band !== "number" && !isNonEmptyString(value.band)) return null; return { rawScore: value.rawScore, band: value.band }; } function readRenderedScoreGuideResult() { var scoreLine = global.document.getElementById("scoreLine"); var bandLine = global.document.getElementById("bandLine"); if (!scoreLine || !bandLine) return null; var scoreMatch = String(scoreLine.textContent || "").match(/(\d+)\s+out of\s+40/i); var bandMatch = String(bandLine.textContent || "").match(/band:\s*([0-9]+(?:\.[0-9]+)?)/i); if (!scoreMatch || !bandMatch) return null; return normaliseScoreGuideResult({ rawScore: Number(scoreMatch[1]), band: bandMatch[1] }); } function captureRenderedScoreGuideResult() { var parsed = readRenderedScoreGuideResult(); if (!parsed) return; latestRenderedScoreGuideResult = parsed; renderedResultVersion += 1; if (elements && !elements.overlay.hidden) { renderScoreGuide(parsed); } } function attachRenderedResultObserver() { if (!global.MutationObserver || resultObserver) return; var scoreLine = global.document.getElementById("scoreLine"); var bandLine = global.document.getElementById("bandLine"); if (!scoreLine || !bandLine) return; resultObserver = new global.MutationObserver(function() { captureRenderedScoreGuideResult(); }); resultObserver.observe(scoreLine, { childList: true, characterData: true, subtree: true }); resultObserver.observe(bandLine, { childList: true, characterData: true, subtree: true }); } function getScoreGuideResult() { var supplied = null; if (validatedConfig && validatedConfig.results && hasFunction(validatedConfig.results, "getScoreGuideResult")) { supplied = normaliseScoreGuideResult(validatedConfig.results.getScoreGuideResult()); if (supplied) return supplied; } if (renderedResultVersion > studySessionResultBaseline) { return latestRenderedScoreGuideResult; } return null; } function parseRange(text) { var match = String(text || "").trim().match(/^(\d+)(?:[–-](\d+))?$/); if (!match) return null; return { min: Number(match[1]), max: match[2] === undefined ? Number(match[1]) : Number(match[2]) }; } function isCurrentScoreRow(row, result) { if (!result) return false; var range = parseRange(row.correctAnswers); return Boolean(range && result.rawScore >= range.min && result.rawScore <= range.max); } function formatBandLabel(band) { var value = String(band); return value.toLowerCase().indexOf("band") === 0 ? value : "Band " + value; } function closeScoreGuideDialog() { if (!elements || !elements.overlay) return; elements.overlay.hidden = true; elements.overlay.setAttribute("aria-hidden", "true"); var focusTarget = previouslyFocusedElement; previouslyFocusedElement = null; if (focusTarget && typeof focusTarget.focus === "function") { focusTarget.focus(); } } function openScoreGuideDialog() { if (!elements || !elements.overlay || elements.root.hidden) return; renderScoreGuide(getScoreGuideResult()); previouslyFocusedElement = global.document.activeElement; elements.overlay.hidden = false; elements.overlay.setAttribute("aria-hidden", "false"); elements.closeButton.focus(); } function handleOverlayKeydown(event) { if (event.key === "Escape") { event.preventDefault(); closeScoreGuideDialog(); } } function handleOverlayClick(event) { if (event.target === elements.overlay) { closeScoreGuideDialog(); } } function buildScoreGuideTable(scoreGuide, result) { var table = createElement("table", "reading-shell-score-guide-table"); table.setAttribute("aria-label", "IELTS Academic Reading raw score conversion"); var thead = createElement("thead", "reading-shell-score-guide-head"); var headRow = createElement("tr", "reading-shell-score-guide-row"); var correctHead = createElement("th", "reading-shell-score-guide-heading", "Correct answers"); var bandHead = createElement("th", "reading-shell-score-guide-heading", "Estimated band"); correctHead.scope = "col"; bandHead.scope = "col"; headRow.append(correctHead, bandHead); thead.append(headRow); var tbody = createElement("tbody", "reading-shell-score-guide-body"); scoreGuide.rows.forEach(function(row) { var current = isCurrentScoreRow(row, result); var bodyRow = createElement("tr", current ? "reading-shell-score-guide-row reading-shell-current-score-row" : "reading-shell-score-guide-row"); var rangeCell = createElement("td", "reading-shell-score-guide-cell", row.correctAnswers); var bandCell = createElement("td", "reading-shell-score-guide-cell", row.band); if (current) { rangeCell.style.background = "rgba(34, 197, 94, 0.14)"; bandCell.style.background = "rgba(34, 197, 94, 0.14)"; rangeCell.style.borderBottomColor = "rgba(34, 197, 94, 0.45)"; bandCell.style.borderBottomColor = "rgba(34, 197, 94, 0.45)"; var badge = createElement("span", "reading-shell-current-score-label", "Your current score"); badge.style.background = "rgba(22, 101, 52, 0.12)"; badge.style.borderRadius = "999px"; badge.style.color = "#166534"; badge.style.display = "inline-block"; badge.style.fontSize = "0.75rem"; badge.style.fontWeight = "800"; badge.style.marginLeft = "8px"; badge.style.padding = "2px 7px"; rangeCell.append(badge); } bodyRow.append(rangeCell, bandCell); tbody.append(bodyRow); }); table.append(thead, tbody); return table; } function renderScoreGuide(result) { if (!elements || !validatedConfig) return; elements.tableWrap.textContent = ""; elements.tableWrap.append(buildScoreGuideTable(validatedConfig.study.scoreGuide, result)); if (result) { elements.summary.textContent = "Your score: " + result.rawScore + " / " + validatedConfig.test.totalQuestions + " · " + formatBandLabel(result.band); elements.summary.hidden = false; } else { elements.summary.textContent = ""; elements.summary.hidden = true; } } function buildUi(config) { var mount = global.document.getElementById("readingFeatureShellMount"); if (!mount) { lastError = "ReadingFeatureShell mount was not found."; global.console.warn("ReadingFeatureShell: " + lastError); initialized = false; validatedConfig = null; return false; } mount.textContent = ""; mount.removeAttribute("aria-hidden"); var root = createElement("div", "reading-shell-root"); root.setAttribute("data-reading-shell-root", ""); root.setAttribute("aria-hidden", "true"); root.hidden = true; var guideButton = createElement("button", "reading-shell-score-guide-button"); guideButton.type = "button"; guideButton.setAttribute("data-reading-shell-score-guide-open", ""); guideButton.setAttribute("aria-haspopup", "dialog"); guideButton.setAttribute("aria-controls", "reading-shell-score-guide-dialog"); var guideIcon = createElement("span", "reading-shell-score-guide-icon", "📊"); guideIcon.setAttribute("aria-hidden", "true"); guideButton.append(guideIcon, createElement("span", "reading-shell-score-guide-label", "Score guide")); var pill = createElement("div", "reading-shell-study-pill", "Study mode"); pill.setAttribute("data-reading-shell-study-pill", ""); pill.setAttribute("role", "status"); pill.setAttribute("aria-label", "Study mode is active"); var timer = createElement("div", "reading-shell-study-timer"); timer.setAttribute("data-reading-shell-study-timer", ""); timer.setAttribute("role", "timer"); var timerLabel = createElement("span", "reading-shell-study-timer-label", "Study time: "); var timerValue = createElement("span", "reading-shell-study-timer-value", "00:00"); timerValue.setAttribute("data-reading-shell-study-timer-value", ""); timer.append(timerLabel, timerValue); var overlay = createElement("div", "reading-shell-score-guide-backdrop"); overlay.setAttribute("data-reading-shell-score-guide-backdrop", ""); overlay.setAttribute("aria-hidden", "true"); overlay.hidden = true; var dialog = createElement("div", "reading-shell-score-guide-dialog"); dialog.id = "reading-shell-score-guide-dialog"; dialog.setAttribute("data-reading-shell-score-guide-dialog", ""); dialog.setAttribute("role", "dialog"); dialog.setAttribute("aria-modal", "true"); dialog.setAttribute("aria-labelledby", "reading-shell-score-guide-title"); dialog.setAttribute("aria-describedby", "reading-shell-score-guide-subtitle"); dialog.setAttribute("tabindex", "-1"); var header = createElement("div", "reading-shell-score-guide-header"); var titleGroup = createElement("div", "reading-shell-score-guide-title-group"); var title = createElement("h2", "reading-shell-score-guide-title", config.study.scoreGuide.title); title.id = "reading-shell-score-guide-title"; var intro = createElement("p", "reading-shell-score-guide-intro", config.study.scoreGuide.intro); intro.id = "reading-shell-score-guide-subtitle"; titleGroup.append(title, intro); var closeButton = createElement("button", "reading-shell-score-guide-close", "×"); closeButton.type = "button"; closeButton.setAttribute("data-reading-shell-score-guide-close", ""); closeButton.setAttribute("aria-label", "Close score guide"); closeButton.setAttribute("title", "Close score guide"); var summary = createElement("p", "reading-shell-score-guide-summary"); summary.style.color = "var(--text)"; summary.style.fontWeight = "800"; summary.style.margin = "0 0 12px"; summary.hidden = true; var tableWrap = createElement("div", "reading-shell-score-guide-scroll"); header.append(titleGroup, closeButton); dialog.append(header, summary, tableWrap); overlay.append(dialog); root.append(guideButton, pill, timer, overlay); mount.append(root); guideButton.addEventListener("click", openScoreGuideDialog); closeButton.addEventListener("click", closeScoreGuideDialog); overlay.addEventListener("click", handleOverlayClick); overlay.addEventListener("keydown", handleOverlayKeydown); elements = { root: root, studyTimerValue: timerValue, guideButton: guideButton, overlay: overlay, closeButton: closeButton, summary: summary, tableWrap: tableWrap }; renderScoreGuide(null); return true; } function sync() { if (!initialized || !validatedConfig || !elements) return; var currentMode = validatedConfig.state.getMode(); var submitted = Boolean(validatedConfig.state.isTestSubmitted()); var visibleInStudyBeforeChecking = currentMode === "study" && !submitted; var visibleInStudyAfterChecking = currentMode === "study" && submitted; var hiddenInTestBeforeSubmission = currentMode === "test" && !submitted; var hiddenInTestAfterSubmission = currentMode === "test" && submitted; var shouldShowStudyChrome = visibleInStudyBeforeChecking || visibleInStudyAfterChecking; if (hiddenInTestBeforeSubmission || hiddenInTestAfterSubmission || currentMode !== "study") { shouldShowStudyChrome = false; } elements.root.hidden = !shouldShowStudyChrome; elements.root.setAttribute("aria-hidden", shouldShowStudyChrome ? "false" : "true"); if (!shouldShowStudyChrome) { studySessionActive = false; stopStudyTimer(); closeScoreGuideDialog(); } else if (!elements.overlay.hidden) { renderScoreGuide(getScoreGuideResult()); } } function startStudySession() { if (!initialized || !validatedConfig || !elements) return; studySessionActive = true; studyElapsedSeconds = 0; studySessionResultBaseline = renderedResultVersion; updateStudyTimerText(); renderScoreGuide(null); startStudyTimer(); sync(); } function init(config) { var validation = validateConfig(config); if (!validation.ok) { validatedConfig = null; initialized = false; elements = null; studySessionActive = false; stopStudyTimer(); lastError = validation.error; global.console.warn("ReadingFeatureShell: " + validation.error); return { ok: false, error: validation.error }; } validatedConfig = config; initialized = true; lastError = ""; studySessionActive = false; studyElapsedSeconds = 0; if (!buildUi(config)) { return { ok: false, error: lastError }; } attachRenderedResultObserver(); updateStudyTimerText(); sync(); return { ok: true, initialized: true }; } function getStatus() { return { initialized: initialized, hasConfig: Boolean(validatedConfig), version: validatedConfig ? validatedConfig.version : null, testId: validatedConfig && validatedConfig.test ? validatedConfig.test.id : "", studySessionActive: studySessionActive, studyElapsedSeconds: studyElapsedSeconds, lastError: lastError }; } global.ReadingFeatureShell = { init: init, sync: sync, startStudySession: startStudySession, getStatus: getStatus, validateConfig: validateConfig }; })(window);